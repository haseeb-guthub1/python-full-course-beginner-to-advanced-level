# write a program to greet  all the perso name stored in a list "l" which starts with H?

l = ["aqib", "Haseeb","hassan","hanan","adan"]
for name in l:
    if(name.startswith("h")):
        
        print(f"Hello {name}")